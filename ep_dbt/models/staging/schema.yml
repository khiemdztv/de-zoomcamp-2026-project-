version: 2

models:
  - name: stg_matches
    columns:
      - name: match_id
        tests:
          - not_null
          - unique
  - name: stg_players
    columns:
      - name: player_name
        tests:
          - not_null